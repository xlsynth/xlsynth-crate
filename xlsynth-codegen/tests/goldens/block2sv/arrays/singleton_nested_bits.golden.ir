package singleton_nested_bits_package

top block singleton_nested_bits(data: bits[1][1][2], row: bits[2], column: bits[2], out: bits[1]) {
  data: bits[1][1][2] = input_port(name=data, id=1)
  row: bits[2] = input_port(name=row, id=2)
  column: bits[2] = input_port(name=column, id=3)
  selected: bits[1] = array_index(data, indices=[row, column], id=4)
  out: () = output_port(selected, name=out, id=5)
}

// OPTIONS: # BlockCodegenOptions defaults
// EXPECT-SV: module singleton_nested_bits(
// EXPECT-SV:   input logic [1:0][0:0] data,
// EXPECT-SV:   input logic [1:0] row,
// EXPECT-SV:   input logic [1:0] column,
// EXPECT-SV:   output logic out
// EXPECT-SV: );
// EXPECT-SV:   logic [0:0] selected__dim0;
// EXPECT-SV:   logic selected;
// EXPECT-SV:   assign selected__dim0 = data[row < 2'h2 ? row : 2'h1];
// EXPECT-SV:   assign selected = selected__dim0[column < 2'h1 ? column : 2'h0];
// EXPECT-SV:   assign out = selected;
// EXPECT-SV: endmodule
