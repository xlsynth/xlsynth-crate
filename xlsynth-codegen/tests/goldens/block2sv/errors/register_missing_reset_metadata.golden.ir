package register_missing_reset_metadata_package

top block register_missing_reset_metadata(clk: clock, reset: bits[1], data: bits[8], out: bits[8]) {
  reg state(bits[8], reset_value=0)
  reset: bits[1] = input_port(name=reset, id=1)
  data: bits[8] = input_port(name=data, id=2)
  state_q: bits[8] = register_read(register=state, id=3)
  state_d: () = register_write(data, register=state, reset=reset, id=4)
  out: () = output_port(state_q, name=out, id=5)
}

// OPTIONS: # BlockCodegenOptions defaults
// EXPECT-ERROR: register `state` in block `register_missing_reset_metadata` has a reset value but the block has no reset port
