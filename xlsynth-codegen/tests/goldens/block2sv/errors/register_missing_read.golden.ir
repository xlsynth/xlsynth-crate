package register_missing_read_package

top block register_missing_read(clk: clock, data: bits[8], out: bits[8]) {
  reg state(bits[8])
  data: bits[8] = input_port(name=data, id=1)
  state_d: () = register_write(data, register=state, id=2)
  out: () = output_port(data, name=out, id=3)
}

// OPTIONS: # BlockCodegenOptions defaults
// EXPECT-ERROR: block 'register_missing_read' register 'state' requires exactly one register_read, found 0
