# SPDX-License-Identifier: Apache-2.0

name: Publish to crates.io
permissions:
  contents: write

on:
  push:
    tags:
      - "v*" # This will trigger the workflow on version tags like v1.0.0, v0.1.0, etc.

jobs:
  publish:
    runs-on: macos-latest
    outputs:
      version_no_v: ${{ steps.version_vars.outputs.version_no_v }}
      patch: ${{ steps.version_vars.outputs.patch }}

    steps:
      - name: Check out code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          # Fetch full history and all tags so gen_version_compat.py can inspect them.
          fetch-depth: 0

      - name: Check version numbers
        run: python scripts/bump_version_numbers.py check

      - name: Set up version variables
        id: version_vars
        run: |
          FULL_VERSION="${GITHUB_REF#refs/tags/}"
          echo "FULL_VERSION=$FULL_VERSION"
          VERSION_NO_V="${FULL_VERSION#v}"
          echo "VERSION_NO_V=$VERSION_NO_V"
          echo "::set-output name=version_no_v::$VERSION_NO_V"
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION_NO_V"
          echo "MAJOR=$MAJOR MINOR=$MINOR PATCH=$PATCH"
          echo "::set-output name=patch::$PATCH"

      - name: Validate version matches
        run: |
          python3 scripts/check_version_is.py "${{ steps.version_vars.outputs.version_no_v }}"

      - name: Download xlsynth binaries
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          python3 -V
          pip3 install --break-system-packages requests
          REQUIRED_TAG=$(python3 scripts/get_required_libxls_dso_and_tools_release_tag.py)
          echo "Required XLS release tag: $REQUIRED_TAG"
          python3 scripts/download_release.py -p arm64 -o xlsynth_tools -v "$REQUIRED_TAG"

      - name: Install protobuf compiler
        run: |
          curl -L --retry 5 --retry-delay 2 --retry-connrefused -o protoc-29.1-osx-aarch_64.zip https://github.com/protocolbuffers/protobuf/releases/download/v29.1/protoc-29.1-osx-aarch_64.zip
          unzip protoc-29.1-osx-aarch_64.zip -d protoc
          sudo mv protoc/bin/protoc /usr/local/bin/protoc
          sudo mv protoc /tmp/
          sudo mv protoc-*.zip /tmp/
          protoc --version

      - name: Install Icarus Verilog
        run: |
          brew update
          brew install icarus-verilog

      - name: Set up Rust
        uses: actions-rs/toolchain@16499b5e05bf2e26879000db0c1d13f7e13fa3af
        with:
          toolchain: stable

      # Release publishing intentionally avoids GitHub Actions caches so the
      # build and test step does not consume mutable repository cache state.

      - name: Build and test
        run: |
          export XLSYNTH_TOOLS=`realpath xlsynth_tools`
          cargo test --workspace

      - name: Install Python dependencies for polling script
        run: python3 -m pip install --break-system-packages requests

      - name: Publish crates in dependency order
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: python3 scripts/publish_crates.py "${{ steps.version_vars.outputs.version_no_v }}"

  post-publish:
    needs: publish
    if: ${{ needs.publish.outputs.patch == '0' }}
    runs-on: macos-latest

    steps:
      - name: Check out main
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          ref: main
          fetch-depth: 0

      - name: Set up post-publish version variables
        id: post_publish_vars
        run: |
          RELEASED_VERSION="${{ needs.publish.outputs.version_no_v }}"
          IFS='.' read -r MAJOR MINOR PATCH <<< "$RELEASED_VERSION"
          NEXT_MINOR=$((MINOR+1))
          NEXT_VERSION="${MAJOR}.${NEXT_MINOR}.0"
          echo "released_version=$RELEASED_VERSION" >> "$GITHUB_OUTPUT"
          echo "next_version=$NEXT_VERSION" >> "$GITHUB_OUTPUT"

      - name: Check current repository version
        id: repo_version_state
        run: |
          RELEASED_VERSION="${{ steps.post_publish_vars.outputs.released_version }}"
          NEXT_VERSION="${{ steps.post_publish_vars.outputs.next_version }}"
          if python3 scripts/check_version_is.py "$NEXT_VERSION"; then
            echo "state=already_bumped" >> "$GITHUB_OUTPUT"
          elif python3 scripts/check_version_is.py "$RELEASED_VERSION"; then
            echo "state=needs_bump" >> "$GITHUB_OUTPUT"
          else
            echo "Repository version is neither released version $RELEASED_VERSION nor expected next version $NEXT_VERSION." >&2
            exit 1
          fi

      - name: Bump version numbers (minor bump for mainline release)
        if: ${{ steps.repo_version_state.outputs.state == 'needs_bump' }}
        run: |
          cargo clean
          python scripts/bump_version_numbers.py bump-minor
          cargo generate-lockfile

      - name: Validate post-bump version equals expected next minor
        if: ${{ steps.repo_version_state.outputs.state == 'needs_bump' }}
        run: |
          NEXT_VERSION="${{ steps.post_publish_vars.outputs.next_version }}"
          echo "Expecting post-bump version: $NEXT_VERSION"
          python3 scripts/check_version_is.py "$NEXT_VERSION"

      - name: Commit bumped version numbers
        if: ${{ steps.repo_version_state.outputs.state == 'needs_bump' }}
        env:
          MY_PAT: ${{ secrets.MY_PAT }}
        run: |
          git config user.name "github-actions"
          git config user.email "xlsynth-github-actions@xlsynth.org"
          git add $(find . -type f -name "Cargo.toml")
          git commit -m "Bump version numbers after successful publish"
          git push "https://${GITHUB_ACTOR}:${MY_PAT}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main

      - name: Update version compatibility docs
        run: |
          python3 scripts/gen_version_compat.py

      - name: Commit version metadata update
        env:
          MY_PAT: ${{ secrets.MY_PAT }}
        run: |
          if git diff --quiet -- docs/version_metadata.md generated_version_compat.json; then
            echo "Version metadata is already up to date."
            exit 0
          fi
          git config user.name "github-actions"
          git config user.email "xlsynth-github-actions@xlsynth.org"
          git add docs/version_metadata.md generated_version_compat.json
          git commit -m "Update version metadata after successful publish"
          git push "https://${GITHUB_ACTOR}:${MY_PAT}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
